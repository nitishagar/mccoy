# Vulnerable MCP fixture

This intentionally insecure, non-malicious MCP server exists only as McCoy's canonical test target.
It contains configuration and description text problems; it performs no harmful actions.
