---
title: MCC003 — Secret reference
description: Catches secret-looking names in the schema or description.
---

**Severity:** High

Tool definitions that reference `api_key`, `secret`, `password`, or `token=` leak the shape of
your secret handling to the model. Remove secret names and require secrets from secure runtime
configuration instead.
