---
title: MCC008 — Unpinned dependency
description: Catches unpinned dependency references in the definition.
---

**Severity:** Low

A description or schema referencing `latest`, `@*`, or `unversioned` pulls in unreviewed versions
at runtime. Pin the dependency to a reviewed version.
